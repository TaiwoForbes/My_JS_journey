//  The while loop is also another way of looping through an array
var incrementor = 0
var text = ""
while(incrementor < 10){
    text += `The incrementor has gone up to ${incrementor} \n`
    incrementor++
}
console.log(text)